# Personal Website (React + TS + Vite)

Two pages: Profile and Videos. Update `src/pages/Videos.tsx` to add/remove video links.

Run locally:

```
npm install
npm run dev
```

Build:

```
npm run build
```

Deploy to GitHub Pages (after setting homepage and remote):

```
npm run deploy
```
